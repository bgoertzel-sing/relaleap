# RelaLeap Automation Status

This file is maintained by the RelaLeap research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Post-Phase 0 HEP next-step selection: command-driven experiment harness, char-level smoke invariants, configurable supervised and PC-style residual smoke training, compact objective and HEP alpha reports with acceptance policy, a checked-in schema v3 local comparison baseline, local/Colab baseline gates, fail-closed live and after-the-fact artifact-contract inspection, per-run child artifact and identity inspection, artifact-contract baseline gating, standard artifacts, temporary real-Chrome Colab bridge support, a concise Phase 0 report, an opt-in pinned-support HEP smoke path, successful visible pinned-support Colab evidence, local paired ordinary-vs-pinned HEP comparison, support-instability diagnostics that expose whether ordinary repicked settling changes column support and whether pinned-vs-repicked logits diverge, an opt-in HEP support-stress config that intentionally produces nonzero support repicking without changing the checked Phase 0 baseline, a paired pinned-support support-stress comparison path with run-level support diagnostics in comparison artifacts, successful real-Chrome Colab evidence for the paired support-stress artifact run, a command-driven local pinned-support decision report that keeps pinned support opt-in under the current evidence, an opt-in clipped HEP settling mechanism probe for support-stress diagnostics, successful real-Chrome Colab evidence for the clipped support-stress HEP comparison, a command-driven clipped-HEP decision report that keeps clipping opt-in under the current evidence, a local opt-in guided clipped HEP oracle probe showing that clipped hidden-state error feedback can improve support-stress loss inside the stability budget, an updated GitHub-backed Colab notebook/bridge evidence contract for guided clipped support-stress validation, successful real-Chrome Colab evidence for the guided clipped HEP oracle comparison, a command-driven guided-clipped oracle decision report that records the oracle success while blocking default promotion because supervised labels are used during settling, an opt-in deployable label-free prediction-entropy clipped HEP probe that is stable but worsens supervised support-stress loss in the local smoke comparison, an opt-in deployable label-free temporal-consistency clipped HEP probe that modestly improves supervised support-stress loss inside the clipped ordinary-logit budget in both local smoke and real-Chrome Colab comparisons, a command-driven temporal-clipped HEP decision report that selects temporal consistency as the current label-free support-stress mitigation candidate while blocking default promotion pending broader evidence, a broader seed-2 temporal-vs-entropy-vs-guided clipped support-stress comparison path with local and real-Chrome Colab evidence that temporal consistency still improves loss inside the stability budgets while entropy worsens loss, seed-3 local plus real-Chrome Colab temporal-vs-entropy-vs-guided clipped support-stress evidence that again selects temporal consistency as the label-free candidate while keeping default promotion blocked, and seed-4 local temporal-vs-entropy-vs-guided clipped support-stress evidence that continues the same pattern pending Colab validation.

## Latest Run

- Status: blocked, 2026-06-19 PDT bounded seed-4 GitHub-backed Colab validation path wiring completed locally but the Colab run cannot use it until the new commit is pushed to `origin/main`.
- Changed: extended the GitHub-backed Colab notebook to run and artifact-check the seed-4 temporal-vs-entropy-vs-guided clipped support-stress comparison; updated the real-Chrome Colab bridge rendered-evidence validator and unit test to require the seed-4 success markers and config IDs; documented the seed-4 Colab command path in `README.md` and `docs/colab_bridge.md`.
- Verified: `./.venv-conda/bin/python -m json.tool notebooks/relaleap_colab_smoke.ipynb >/dev/null` passed; `./.venv-conda/bin/python -m unittest tests.test_colab_playwright_runner` passed with 4 tests; `./.venv-conda/bin/python -m unittest discover -s tests` passed with 53 tests. `./.venv-conda/bin/python -m pytest tests/test_colab_playwright_runner.py` was attempted but `pytest` is not installed in `.venv-conda`.
- Evidence: before this run, `git ls-remote origin refs/heads/main` matched local `HEAD` at `970cb972ea073596b7a0715c3cba84ced44d5b9f`; after the local seed-4 Colab notebook/bridge edits, the GitHub-backed notebook URL would still load the old notebook until the new commit is pushed by the separate auto-push helper, so running the Chrome CDP bridge now would duplicate the seed-3-era notebook rather than validate seed 4.
- Blockers: GitHub-backed Colab runtime state depends on `origin/main`; the required seed-4 notebook/bridge commit is local pending auto-push.
- Next step: after the auto-push helper exposes this commit on `origin/main`, run `./.venv-conda/bin/python tools/colab_playwright_runner.py --cdp-url http://127.0.0.1:9222 --run-all --run-method shortcut --wait-completion --debug-snapshot`.
