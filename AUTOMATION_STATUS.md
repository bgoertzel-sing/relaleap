# RelaLeap Automation Status

This file is maintained by the RelaLeap research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase 0 infrastructure: command-driven experiment harness, char-level smoke invariants, configurable supervised and PC-style residual smoke training, compact objective and HEP alpha reports with acceptance policy, a checked-in local comparison baseline, a local/Colab baseline gate, after-the-fact baseline artifact inspection, standard artifacts, and temporary Colab bridge support.

## Latest Run

- Status: ok, 2026-06-19 automation run at 2026-06-19T12:11:02Z
- Changed: added after-the-fact baseline reference checking to `python -m relaleap.experiments.check_artifacts`. The checker can now compare an existing comparison `summary.json` against `baselines/phase0_char_smoke_comparison.json` without rerunning experiments and without requiring that the original run wrote `baseline_comparison.json`. Factored the stable baseline comparison helper in `relaleap.experiments.compare`, added drift/pass tests, and updated `README.md` plus `docs/colab_bridge.md` with the stronger post-Colab inspection command.
- Verified: `python -m compileall relaleap tests tools`; `python -m unittest tests.test_check_artifacts tests.test_compare`; `git diff --check`; `python -m unittest tests.test_smoke tests.test_compare tests.test_check_artifacts` passed with 4 torch-dependent skips under bare Python; `.venv-conda/bin/python -m unittest tests.test_smoke tests.test_compare tests.test_check_artifacts` passed; `.venv-conda/bin/python -m relaleap.experiments.compare --out /tmp/relaleap-checker-baseline-reference --baseline-reference baselines/phase0_char_smoke_comparison.json` passed; `.venv-conda/bin/python -m relaleap.experiments.check_artifacts --comparison-dir /tmp/relaleap-checker-baseline-reference --baseline-reference baselines/phase0_char_smoke_comparison.json --out /tmp/relaleap-checker-baseline-reference/artifact_check.json` passed; `.venv-conda/bin/python -m relaleap.experiments.compare --out /tmp/relaleap-checker-after-fact` passed; `.venv-conda/bin/python -m relaleap.experiments.check_artifacts --comparison-dir /tmp/relaleap-checker-after-fact --baseline-reference baselines/phase0_char_smoke_comparison.json --out /tmp/relaleap-checker-after-fact/artifact_check.json` passed with no prewritten `baseline_comparison.json`, checker `status: pass`, comparison `status: ok`, verdict `status: pass`, baseline reference comparison `status: pass`, 12 Phase 0 invariants checked/passed, accepted HEP alpha `0.25`, and no artifact failures.
- Blockers: live Colab/GPU execution was not completed. The local headless bridge attempt failed before opening Colab because Chromium was blocked by macOS sandbox state (`TargetClosedError` after `MachPortRendezvousServer ... Permission denied`). Existing `.venv-conda` still emits the known NumPy 2.4.6 / torch 2.2.2 ABI warning on import; execution still succeeds, and `pyproject.toml` already pins fresh installs to `numpy<2`.
- Next step: manually open the GitHub-backed Colab notebook, choose a GPU runtime, run all cells, then run `python -m relaleap.experiments.check_artifacts --comparison-dir results/comparisons/colab_phase0 --baseline-reference baselines/phase0_char_smoke_comparison.json`.
