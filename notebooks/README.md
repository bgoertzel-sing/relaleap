# Notebooks

Colab notebooks in this directory should be thin wrappers around repository commands.

They should clone or pull the GitHub repo, install dependencies, run a config-driven experiment, and save artifacts. They should not become the source of truth for experiment logic.

